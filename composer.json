{
  "require-dev": {
    "symfony/var-dumper": "^7.1"
  },
  "require": {
    "symfony/dotenv": "^7.1",
    "php": ">=8.2",
    "ext-mysqli": "*"
  },
  "scripts": {
    "pre-install-cmd": [
      "composer check-platform-reqs",
      "cp -n .env.dist .env"
    ]
  }
}
