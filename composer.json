{
  "require-dev": {
    "symfony/var-dumper": "^7.2",
    "rector/rector": "^2.0"
  },
  "require": {
    "symfony/dotenv": "^7.2",
    "php": ">=8.2",
    "ext-mysqli": "*"
  },
  "scripts": {
    "pre-install-cmd": ["composer check-platform-reqs", "cp -n .env.dist .env"],
    "ci": ["rector --dry-run --no-progress-bar --no-ansi"]
  }
}
