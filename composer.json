{
  "require-dev": {
    "symfony/var-dumper": "^7.2"
  },
  "require": {
    "symfony/dotenv": "^7.2",
    "php": ">=8.2",
    "ext-mysqli": "*"
  },
  "scripts": {
    "pre-install-cmd": ["composer check-platform-reqs", "cp -n .env.dist .env"]
  }
}
